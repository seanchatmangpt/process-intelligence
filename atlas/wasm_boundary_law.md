# WASM Boundary Law: Specta + tsify

The Ostar Generative Pipeline enforces a strict separation between type definition and binary execution.
Specta governs the Type Surface (TS Control), while tsify/wasm-bindgen governs the Binary Surface (WASM ABI), ensuring stability and receipt integrity across the membrane.