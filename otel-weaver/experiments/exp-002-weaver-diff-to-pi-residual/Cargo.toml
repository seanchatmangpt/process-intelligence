[package]
name = "exp-002-weaver-diff-to-pi-residual"
version = "0.1.0"
edition = "2021"

[lib]
name = "bridge"
path = "src/bridge.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
